{
  "name": "lost-found-midi-editor",
  "version": "2.0.0",
  "description": "MIDI Editor for Chase Bliss Lost + Found pedal",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  },
  "dependencies": {
    "firebase": "^12.8.0"
  }
}
